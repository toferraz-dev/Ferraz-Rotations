version: 3.3
author: Ferraz
patch: "12.0.1"
spec: 104
name: Guardian Elunes
description: |
  Master Guardian Druid Rotation for Mythic+ (Midnight Expansion).
  Exclusively optimized for Elunes Chosen Hero Talents.

entry: main

# ==========================================================
#  CONFIGURATION
# ==========================================================

config:

  survival_instincts_threshold:
    label: Survival Instincts %
    type: slider
    min: 10
    max: 70
    default: 50

  frenzied_regen_threshold:
    label: Frenzied Regeneration %
    type: slider
    min: 30
    max: 80
    default: 65

  barkskin_threshold:
    label: Barkskin %
    type: slider
    min: 30
    max: 90
    default: 65

  ironfur_stacks_max:
    label: "Max Ironfur Stacks (heavy damage)"
    type: slider
    min: 2
    max: 10
    default: 3

  allow_pull_with_mo:
    label: "Pull with Moonfire"
    type: checkbox
    default: true

  raze_targets:
    label: "Min targets for Raze (Elune)"
    type: slider
    min: 2
    max: 5
    default: 2

  incarnation_enemies:
    label: "Min enemies for Incarnation"
    type: slider
    min: 1
    max: 10
    default: 3

# ==========================================================
#  VARIABLES
# ==========================================================

variables:

  in_bear_form: buff.bear_form.up|player.form=1
  rage: player.rage
  rage_overcap: player.rage>=85
  ironfur_stacks: buff.ironfur.stack
  
  incarnation_active: buff.incarnation_guardian_of_ursoc.up|buff.berserk.up
  galactic_guardian_up: buff.galactic_guardian.up
  tooth_and_claw_up: buff.tooth_and_claw.up
  gore_up: buff.gore.up
  harnessed_rage: talent.harnessed_rage
  
  thrash_needs_refresh: debuff.thrash.remains<4.5|debuff.thrash.stack<3
  moonfire_needs_refresh: debuff.moonfire.remains<4.8

  lunar_beam_ready: cooldown.lunar_beam.ready
  use_lunar_beam: var.lunar_beam_ready&(active_enemies>=2|health.pct<70|target.boss)
  use_raze: active_enemies>=config.raze_targets
  
  any_major_defensive: buff.barkskin.up|buff.survival_instincts.up|buff.rage_of_the_sleeper.up

# ==========================================================
#  ACTION LISTS
# ==========================================================

lists:

  sanity_checks:
    - return,if=player.dead
    - return,if=mounted
    - return,if=!state.rotation
    - return,if=player.cc

  interrupts:
    - skull_bash,name="Skull Bash",if=interrupt.target.check
    - skull_bash.mouseover,name="Skull Bash (MO)",if=interrupt.mouseover.check
    - incapacitating_roar,name="Roar (AoE CC)",if=interrupt.stun.aoe.check
    - mighty_bash,name="Mighty Bash",if=interrupt.cc.check
    - soothe,name="Soothe Enrage",if=target.purgeable.enrage

  defensives:
    # EMERGENCY PANIC (Priority Survival)
    - survival_instincts,name="Survival Instincts (Panic)",if=health.pct<15&!buff.survival_instincts.up
    - rage_of_the_sleeper,name="Rale Sleeper (Panic)",if=health.pct<20&!buff.survival_instincts.up
    - frenzied_regeneration,name="Frenzied Regen (Panic)",if=health.pct<config.frenzied_regen_threshold&player.rage>=10
    - barkskin,name="Barkskin",if=health.pct<config.barkskin_threshold&!buff.survival_instincts.up
    - survival_instincts,name="Survival Instincts",if=health.pct<config.survival_instincts_threshold&!var.any_major_defensive

  cooldowns:
    - wild_guardian,name="Wild Guardian (Apex)",if=var.incarnation_active
    - lunar_beam,name="Lunar Beam",if=var.use_lunar_beam&target.range<=10
    - incarnation_guardian_of_ursoc,name="Incarnation",if=active_enemies>=config.incarnation_enemies|(target.boss&target.time_to_die>20)
    - berserk_ravage,name="Berserk (Elune)",if=talent.berserk&(active_enemies>=3|target.boss)
    - rage_of_the_sleeper,name="Rage of the Sleeper",if=health.pct<70|active_enemies>=3|var.incarnation_active

  rotation_st_elune:
    - moonfire,name="Moonfire (Guardian Proc)",if=var.galactic_guardian_up
    - mangle,name="Mangle (Proc)",if=var.gore_up
    - mangle,name="Mangle (Charges)",if=var.harnessed_rage&cooldown.mangle.charges_fractional>=1.8
    - mangle,name="Mangle Filler"
    - thrash,name="Thrash Maintenance",if=debuff.thrash.stack<3
    - moonfire,name="Moonfire (Refresh)",if=var.moonfire_needs_refresh
    - raze,name="Raze (Dump)",if=active_enemies=1&player.rage>=90
    - swipe,name="Swipe Filler"

  rotation_aoe_elune:
    - moonfire,name="Moonfire (Guardian Proc)",if=var.galactic_guardian_up
    - thrash,name="Thrash Refresh AoE",if=var.thrash_needs_refresh
    - raze,name="Raze (Beam Sync)",if=buff.lunar_beam.up&player.rage>=30
    - call_action_list,name=moonfire_spread,if=var.moonfire_needs_refresh|active_enemies>1&debuff.moonfire.down
    - mangle,name="Mangle Proc AoE",if=var.gore_up
    - thrash,name="Thrash AoE"
    - mangle,name="Mangle AoE"
    - raze,name="Raze (Harnessed)",if=player.rage>=80
    - raze,name="Raze Dump AoE",if=player.rage>=40
    - swipe,name="Swipe AoE Filler"

  moonfire_spread:
    - moonfire,name="Moonfire Start",if=debuff.moonfire.down&range<=40
    - moonfire,name="Moonfire (Galactic)",if=var.galactic_guardian_up
    - moonfire,name="Moonfire Refresh",if=var.moonfire_needs_refresh
    - moonfire,mouseover,name="Moonfire (MO)",if=debuff.moonfire.down&range<=40&target.alive&target.enemy

  rage_management:
    - ironfur,name="Ironfur (Resource Dump)",if=var.ironfur_stacks<config.ironfur_stacks_max&player.rage>=85
    - maul,name="Maul (Tooth/Claw)",if=var.tooth_and_claw_up&player.rage>=40&!var.use_raze
    - raze,name="Raze (Dump)",if=var.use_raze&player.rage>=80
    - ironfur,name="Ironfur (Lower Fallback)",if=var.ironfur_stacks<1&player.rage>=45

  # ==========================================================
  #  MAIN ENTRY POINT
  # ==========================================================

  main:
    - call_action_list,name=sanity_checks

    # --- FORCED IRONFUR (FORCE AT ALL COSTS) ---
    - ironfur,name="IF_URGENT",if=player.rage>=40&buff.ironfur.stack<1
    - ironfur,name="IF_HEAVY",if=player.rage>=65&buff.ironfur.stack<2

    - bear_form,name="Manual Bear Form",if=!var.in_bear_form&player.combat&!mounted
    - moonfire,name="Pull Moonfire",if=config.allow_pull_with_mo&!player.combat&target.enemy&target.alive&debuff.moonfire.down
    
    - call_action_list,name=interrupts
    - call_action_list,name=defensives,if=var.in_bear_form
    - call_action_list,name=cooldowns,if=var.in_bear_form
    
    - call_action_list,name=rotation_aoe_elune,if=active_enemies>=2&var.in_bear_form
    - call_action_list,name=rotation_st_elune,if=var.in_bear_form
    - call_action_list,name=rage_management,if=var.in_bear_form