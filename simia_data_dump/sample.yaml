# DEATH KNIGHT - BLOOD

version: 3

config:
  sample:
    label: "sample"
    type: slider
    min: 0
    max: 90
    default: 50

  

variables:
  test_variable: health.pct<cfg.sample

lists:

  nested:
    - sample_action,if=test_variable

  main:
    # _shared.yaml lists
    - call_action_list,name=spell_queue
    - call_action_list,name=sanity_checks
    - call_action_list,name=auto_target