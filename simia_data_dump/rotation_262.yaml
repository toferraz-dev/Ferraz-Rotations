# =============================================================================
# Shaman Elemental - Spec ID 262
# =============================================================================

version: 3
spec: 262
entry: main

movement_allowed: buff.spiritwalkers_grace.up

config:
  astral_shift_hp_pct:
    section: "Defensive HP Percentage"
    label: "Astral Shift HP%"
    type: slider
    min: 0
    max: 100
    default: 50

  astral_shift_dmg_pct:
    section: "Defensives Prediction Damage"
    label: "Astral Shift (Incoming Dmg%)"
    type: slider
    min: 0
    max: 100
    default: 50

  healing_surge_OOC_hp_pct:
    label: "Healing Surge OOC HP%"
    type: slider
    min: 0
    max: 100
    default: 60

  ghost_wolf_mode:
    label: "Auto Ghost Wolf"
    type: dropdown
    options:
      - "Off"
      - "Out of Combat"
      - "OOC + Far Target"
    default: 1

variables:

lists:
  defensives:
    # --- Prediction Damage: pre-fire on a predicted incoming hit (% of effective HP) ---
    - astral_shift,range_check=none,if=incoming.mitigated.pct>=config.astral_shift_dmg_pct

    # --- HP Percentage: react when already low ---
    - ancestral_protection,range_check=none,if=health.pct<=config.astral_shift_hp_pct
    - astral_shift,range_check=none,if=health.pct<=config.astral_shift_hp_pct

  aoe:
    # --- 3+ TARGET ROTATION ---
    # stormkeeper,if=cooldown.ascendance.remains>10|cooldown.ascendance.remains<gcd|fight_remains<20
    - stormkeeper,if=cooldown.ascendance.remains>10|cooldown.ascendance.remains<gcd|fight_remains<20
    # ancestral_swiftness
    - ancestral_swiftness
    # flame_shock,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2))&talent.master_of_the_elements&talent.inferno_arc&spell_targets.chain_lightning=3
    - flame_shock,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2))&talent.master_of_the_elements&talent.inferno_arc&enemies_around_target=3
    # voltaic_blaze,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2)|talent.purging_flames)
    - voltaic_blaze,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2)|talent.purging_flames)
    # ascendance,if=cooldown.stormkeeper.remains>15|fight_remains<20
    - ascendance,if=cooldown.stormkeeper.remains>15|fight_remains<20
    # elemental_blast,target_if=min:debuff.lightning_rod.remains,if=buff.tempest.stack<2&(buff.elemental_blast_critical_strike.up+buff.elemental_blast_haste.up+buff.elemental_blast_mastery.up=0)
    # Original target_if: min:debuff.lightning_rod.remains
    - elemental_blast,if=buff.tempest.stack<2&(buff.elemental_blast_critical_strike.up+buff.elemental_blast_haste.up+buff.elemental_blast_mastery.up=0)
    # earthquake,if=buff.tempest.stack<2&lightning_rod<active_enemies&spell_targets.chain_lightning>=3+talent.elemental_blast&(buff.elemental_blast_critical_strike.up+buff.elemental_blast_haste.up+buff.elemental_blast_mastery.up>0|!talent.elemental_blast)
    - earthquake,if=buff.tempest.stack<2&active_dot.lightning_rod<enemies_around_target&enemies_around_target>=3+talent.elemental_blast&(buff.elemental_blast_critical_strike.up+buff.elemental_blast_haste.up+buff.elemental_blast_mastery.up>0|!talent.elemental_blast)
    # elemental_blast,target_if=min:debuff.lightning_rod.remains,if=buff.tempest.stack<2&spell_targets.chain_lightning=3
    # Original target_if: min:debuff.lightning_rod.remains
    - elemental_blast,if=buff.tempest.stack<2&enemies_around_target=3
    # lava_burst,if=buff.purging_flames.up&(buff.lava_surge.up|cooldown.voltaic_blaze.remains<2)
    - lava_burst,if=buff.purging_flames.up&(buff.lava_surge.up|cooldown.voltaic_blaze.remains<2)
    # lava_burst,if=buff.tempest.up&buff.lava_surge.up&talent.master_of_the_elements&spell_targets.chain_lightning=3
    - lava_burst,if=buff.tempest.up&buff.lava_surge.up&talent.master_of_the_elements&enemies_around_target=3
    # tempest,target_if=min:debuff.lightning_rod.remains,if=buff.master_of_the_elements.up
    # Original target_if: min:debuff.lightning_rod.remains
    - tempest,if=buff.master_of_the_elements.up
    # tempest,target_if=min:debuff.lightning_rod.remains,if=buff.stormkeeper.stack<4&buff.tempest.stack=2
    # Original target_if: min:debuff.lightning_rod.remains
    - tempest,if=buff.stormkeeper.stack<4&buff.tempest.stack=2
    # chain_lightning,if=buff.stormkeeper.up&maelstrom.deficit>(spell_targets.chain_lightning>?5)*(2+(spell_targets.chain_lightning>?5)+2)
    - chain_lightning,if=buff.stormkeeper.up&maelstrom.deficit>(enemies_around_target>?5)*(2+(enemies_around_target>?5)+2)
    # earthquake,if=!talent.elemental_blast&maelstrom.deficit<15+(buff.stormkeeper.up*(spell_targets.chain_lightning>?5)*(2+(spell_targets.chain_lightning>?5)))
    - earthquake,if=!talent.elemental_blast&maelstrom.deficit<15+(buff.stormkeeper.up*(enemies_around_target>?5)*(2+(enemies_around_target>?5)))
    # elemental_blast
    - elemental_blast
    # tempest,target_if=min:debuff.lightning_rod.remains
    # Original target_if: min:debuff.lightning_rod.remains
    - tempest
    # chain_lightning
    - chain_lightning
    # flame_shock,moving=1
    - flame_shock,moving=1
    # voltaic_blaze,moving=1
    - voltaic_blaze,moving=1
    # frost_shock,moving=1
    - frost_shock,moving=1

  single_target:
    # --- 1 and 2 TARGET ROTATION ---
    # stormkeeper,if=cooldown.ascendance.remains>10|cooldown.ascendance.remains<gcd|fight_remains<20
    - stormkeeper,if=cooldown.ascendance.remains>10|cooldown.ascendance.remains<gcd|fight_remains<20
    # ancestral_swiftness
    - ancestral_swiftness
    # flame_shock,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2))
    - flame_shock,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2))
    # voltaic_blaze,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2)|talent.purging_flames&spell_targets.chain_lightning=2)
    - voltaic_blaze,if=!buff.master_of_the_elements.up&((dot.flame_shock.refreshable&cooldown.ascendance.remains>5)|(buff.fire_elemental.up&buff.fire_elemental.remains<2)|talent.purging_flames&enemies_around_target=2)
    # ascendance,if=cooldown.stormkeeper.remains>15|fight_remains<20
    - ascendance,if=cooldown.stormkeeper.remains>15|fight_remains<20
    # lava_burst,if=!buff.master_of_the_elements.up&maelstrom.deficit>15&(cooldown.lava_burst.charges_fractional>1.8|!buff.call_of_the_ancestors.up|spell_targets.chain_lightning=1)&(talent.master_of_the_elements|(talent.molten_wrath&cooldown.lava_burst.charges_fractional>0.8+talent.elemental_reverb)|buff.lava_surge.up|(talent.fusion_of_elements|talent.call_of_the_ancestors)&(!buff.storm_elemental.up|buff.wind_gust.stack=4)|buff.purging_flames.up&spell_targets.chain_lightning=2&(buff.lava_surge.up|cooldown.voltaic_blaze.remains<2))
    - lava_burst,if=!buff.master_of_the_elements.up&maelstrom.deficit>15&(cooldown.lava_burst.charges_fractional>1.8|!buff.call_of_the_ancestors.up|enemies_around_target=1)&(talent.master_of_the_elements|(talent.molten_wrath&cooldown.lava_burst.charges_fractional>0.8+talent.elemental_reverb)|buff.lava_surge.up|(talent.fusion_of_elements|talent.call_of_the_ancestors)&(!buff.storm_elemental.up|buff.wind_gust.stack=4)|buff.purging_flames.up&enemies_around_target=2&(buff.lava_surge.up|cooldown.voltaic_blaze.remains<2))
    # tempest,if=buff.master_of_the_elements.up|!talent.master_of_the_elements
    - tempest,if=buff.master_of_the_elements.up|!talent.master_of_the_elements
    # lightning_bolt,if=buff.stormkeeper.up&(buff.master_of_the_elements.up|!talent.master_of_the_elements)
    - lightning_bolt,if=buff.stormkeeper.up&(buff.master_of_the_elements.up|!talent.master_of_the_elements)
    # elemental_blast,target_if=min:debuff.lightning_rod.remains
    # Original target_if: min:debuff.lightning_rod.remains
    - elemental_blast
    # earth_shock,target_if=min:debuff.lightning_rod.remains
    # Original target_if: min:debuff.lightning_rod.remains
    - earth_shock
    # tempest
    - tempest
    # chain_lightning,if=talent.call_of_the_ancestors&spell_targets.chain_lightning=2
    - chain_lightning,if=talent.call_of_the_ancestors&enemies_around_target=2
    # lightning_bolt
    - lightning_bolt
    # flame_shock,moving=1
    - flame_shock,moving=1
    # voltaic_blaze,moving=1
    - voltaic_blaze,moving=1
    # frost_shock,moving=1
    - frost_shock,moving=1

  main:
    # _shared.yaml lists
    - call_action_list,name=spell_queue
    - call_action_list,name=sanity_checks
    - call_action_list,name=auto_target

    - skyfury,range_check=none,if=(buff.skyfury.down.any|action.skyfury.overlayed)&lastcast.skyfury>=10
    - lightning_shield,range_check=none,if=buff.lightning_shield.down
    - flametongue_weapon,range_check=none,if=mainhand.enchant.remains<600
    - thunderstrike_ward,range_check=none,if=buff.thunderstrike_ward.down
    - healing_surge,range_check=none,if=health.pct<=config.healing_surge_OOC_hp_pct&!player.combat

    # Auto Ghost Wolf - Out of Combat
    - ghost_wolf,range_check=none,if=(config.ghost_wolf_mode=1|config.ghost_wolf_mode=2)&!player.combat&player.moving.time>1&buff.ghost_wolf.down

    # Combat Check
    - return,if=auto_combat.player

    # Defensives
    - call_action_list,name=defensives

    - wind_shear,if=interrupt.target.check
    - wind_shear.mouseover,if=interrupt.mouseover.check
    - wind_shear.focus,if=interrupt.focus.check
    # AoE Interrupt
    - capacitor_totem.player,if=interrupt.stun.aoe.check
    # Racial Interrupts
    - quaking_palm,if=interrupt.cc.check
    - haymaker,if=interrupt.cc.check
    - war_stomp,if=interrupt.stun.aoe.check
    - bull_rush,if=interrupt.stun.aoe.check
    - tail_swipe,if=interrupt.stun.aoe.check

    - call_action_list,name=auto_heal

    # Target Check
    - return,if=auto_combat.target

    # Auto Ghost Wolf - Far Target (in combat)
    - ghost_wolf,range_check=none,if=config.ghost_wolf_mode=2&target.range>40&player.moving.time>1&buff.ghost_wolf.down

    # spiritwalkers_grace,moving=1,if=movement.distance>6
    - spiritwalkers_grace,if=player.moving.time>1
    # wind_shear
    # (already handled above in interrupts section)
    # blood_fury
    - blood_fury
    # berserking
    - berserking
    # fireblood
    - fireblood
    # ancestral_call
    - ancestral_call

    - trinket_1,if=trinket_1.sync
    - trinket_2,if=trinket_2.sync
    # lightning_shield,if=buff.lightning_shield.down
    # (already handled above in precombat/buffs section)
    # natures_swiftness
    - natures_swiftness
    # invoke_external_buff,name=power_infusion — not supported, removed
    # potion,if=buff.bloodlust.up|cooldown.ascendance.ready&cooldown.stormkeeper.remains>15|fight_remains<31 — not supported, removed
    # run_action_list,name=aoe,if=spell_targets.chain_lightning>=3
    - run_action_list,name=aoe,if=enemies_around_target>=3
    # run_action_list,name=single_target
    - run_action_list,name=single_target
