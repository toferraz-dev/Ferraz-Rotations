version: 4.0
author: Ferraz
patch: "12.0.5"
spec: 104
name: Ferraz Guardian Druid M+ (Elunes)
description: |
  Ferraz Guardian Druid M+ (Elunes)
  Talent Tree: CgGAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAgZmxsMMjZWMLzMzMWGY2MMaimZmlxMzMLzMzDMAAAAAADbzAW2mZwYWGATAAAAbYGALmBDGLAwMDgB
entry: main

config:
  # -- Rotation --
  min_aoe_enemies:
    label: "Min enemies for AOE"
    section: "Rotation"
    type: slider
    min: 1
    max: 10
    default: 1

  incarnation_enemies:
    label: "Min enemies for Incarnation"
    section: "Rotation"
    type: slider
    min: 1
    max: 10
    default: 3

  # -- Defensives --
  survival_instincts_threshold:
    label: Survival Instincts %
    section: "Defensives"
    type: slider
    min: 10
    max: 70
    default: 45

  frenzied_regen_threshold:
    label: Frenzied Regeneration %
    section: "Defensives"
    type: slider
    min: 30
    max: 80
    default: 65

  use_barkskin_on_cd:
    label: "Barkskin on Cooldown"
    section: "Defensives"
    type: checkbox
    default: true

  ironfur_stacks_min:
    label: "Min Ironfur Stacks"
    section: "Defensives"
    type: slider
    min: 1
    max: 5
    default: 2

  ironfur_stacks_max:
    label: "Max Ironfur Stacks"
    section: "Defensives"
    type: slider
    min: 3
    max: 10
    default: 5

  # -- Movement --
  stampeding_roar_usage:
    label: "Stampeding Roar Usage"
    section: "Movement"
    type: dropdown
    options:
      - label: "Disabled"
        value: 0
      - label: "Only in combat"
        value: 1
      - label: "Only out of combat"
        value: 2
      - label: "Always when moving"
        value: 3
    default: 2

  stampeding_roar_movement_time:
    label: "Roar after moving (seconds)"
    section: "Movement"
    type: slider
    min: 0
    max: 4
    default: 2

  # -- Utility --
  use_growl:
    label: "Auto Growl (Taunt)"
    section: "Utility"
    type: checkbox
    default: true


  use_mark_of_the_wild:
    label: "Auto Mark of the Wild"
    section: "Utility"
    type: checkbox
    default: true

  # -- Trinkets --
  use_trinket_1:
    label: "Auto Trinket 1"
    section: "Trinkets"
    type: checkbox
    default: false

  use_trinket_2:
    label: "Auto Trinket 2"
    section: "Trinkets"
    type: checkbox
    default: false

  use_potion:
    label: "Auto Offensive Potion"
    section: "Trinkets"
    type: checkbox
    default: false

variables:
  # Form and buff state
  in_bear_form: buff.bear_form.up
  incarnation_active: buff.incarnation_guardian_of_ursoc.up|buff.berserk.up
  thrash_refresh: target.debuff.remains(thrash)<4.5|target.debuff.stacks(thrash)<5
  moonfire_refresh: target.debuff.remains(moonfire)<3
  # Stampeding Roar conditions based on config
  use_stampeding_roar: moving.time>=config.stampeding_roar_movement_time&buff.stampeding_roar.down&((config.stampeding_roar_usage=1&player.combat)|(config.stampeding_roar_usage=2&!player.combat)|(config.stampeding_roar_usage=3))

lists:
  # Mark of the Wild out of combat
  out_of_combat:
    - mark_of_the_wild,name="Mark of the Wild",if=config.use_mark_of_the_wild&group.count(cycle.buff.mark_of_the_wild.down.any)>0

  # Defensive priority: Dream of Cenarius Regrowth > SI > Frenzied Regen > Barkskin (on CD)
  defensives:
    - regrowth,name="Regrowth (Guardian)",if=talent.dream_of_cenarius&buff.dream_of_cenarius.up&health.effective.pct<85
    - survival_instincts,name="Survival Instincts",if=health.effective.pct<config.survival_instincts_threshold&!buff.survival_instincts.up
    - frenzied_regeneration,name="Frenzied Regeneration",if=health.effective.pct<config.frenzied_regen_threshold
    - barkskin,name="Barkskin",if=config.use_barkskin_on_cd&!buff.survival_instincts.up
    - barkskin,name="Barkskin (Low HP)",if=!config.use_barkskin_on_cd&health.effective.pct<60&!buff.survival_instincts.up

  # Racial damage CDs (unconditional)
  racials:
    - blood_fury,name="Blood Fury"
    - berserking,name="Berserking"
    - fireblood,name="Fireblood"
    - ancestral_call,name="Ancestral Call"

  # Auto-swap to enraged target; Skull Bash on target/MO/focus; AoE CC; Soothe
  interrupts:
    - target_enemy,delay=100,name="Search Soothe",if=!target.purgeable.enrage&enemies.combat.any(cycle.purgeable.enrage)
    - skull_bash,name="Skull Bash",if=interrupt.target.check
    - skull_bash.mouseover,name="Skull Bash (MO)",if=interrupt.mouseover.check
    - skull_bash.focus,name="Skull Bash (Focus)",if=interrupt.focus.check
    - incapacitating_roar,name="Roar (AoE CC)",if=interrupt.stun.aoe.check
    - soothe,name="Soothe Enrage",if=target.purgeable.enrage

  # Taunt: grab loose mobs (same pattern as Monk Provoke)
  taunt:
    - growl,name="Growl",if=config.use_growl&target.threat<2&target.combat&target.threat!=-1&!player.inraid
    - growl.mouseover,name="Growl (MO)",if=config.use_growl&mouseover.threat<2&mouseover.combat&mouseover.threat!=-1&mouseover.exists&!player.inraid

  # Innervate healer + Stampeding Roar
  utility:
    - stampeding_roar,name="Stampeding Roar",if=var.use_stampeding_roar&config.stampeding_roar_usage!=0

  # Trinkets > Potion > Lunar Beam > Sundering Roar > Incarnation/Berserk > Wild Guardian
  cooldowns:
    # Trinkets and Potion: sync with Incarnation burst window
    - trinket_1,name="Trinket 1",if=config.use_trinket_1&trinket_1.sync&var.incarnation_active
    - trinket_2,name="Trinket 2",if=config.use_trinket_2&trinket_2.sync&var.incarnation_active
    - combat_potion,name="Combat Potion",if=combat_potion.ready&config.use_potion&var.incarnation_active
    # Lunar Beam: use aggressively, almost on CD (24 uses vs 15 Inc in reference log)
    - lunar_beam,name="Lunar Beam",if=active_enemies>=1
    - sundering_roar,name="Sundering Roar",if=talent.sundering_roar&debuff.thrash.stack<3
    # Incarnation/Berserk: require min enemies or boss target; never overlap (ref log spaces ~110s apart)
    - incarnation_guardian_of_ursoc,name="Incarnation",if=!var.incarnation_active&(active_enemies>=config.incarnation_enemies|target.boss)
    - berserk,name="Berserk",if=!var.incarnation_active&!talent.incarnation_guardian_of_ursoc&(active_enemies>=config.incarnation_enemies|target.boss)
    - wild_guardian,name="Wild Guardian"

  # Elune's Chosen rotation
  # Focuses on high Moonfire/Thrash/Mangle uptime and Raze as primary AoE spender.
  rotation_elunes:
    # Ironfur: proactive maintenance
    - ironfur,name="Ironfur (Maintain)",if=buff.ironfur.remains<3|buff.ironfur.stack<config.ironfur_stacks_min
    - ironfur,name="Ironfur (Heavy Damage)",if=health.effective.pct<50&buff.ironfur.stack<config.ironfur_stacks_max

    # Raze: spend Tooth and Claw procs immediately (free + more damage + defensive)
    - raze,name="Raze (Proc)",if=buff.tooth_and_claw.up

    # Raze: primary rage spender when defense is safe
    - raze,name="Raze",if=rage>=50&buff.ironfur.stack>=config.ironfur_stacks_min

    # Galactic Guardian Moonfire — high priority to avoid wasting procs
    - moonfire,name="Moonfire (GG)",if=buff.galactic_guardian.up&var.in_bear_form

    # Thrash: maintain 3 stacks and bleed
    - thrash,name="Thrash (Refresh)",target_if=var.thrash_refresh

    # Mangle: core generator
    - mangle,name="Mangle"

    # Thrash: core AoE filler
    - thrash,name="Thrash"

    # Fillers: Moonfire > Swipe
    - moonfire,name="Moonfire (Refresh)",if=var.moonfire_refresh&var.in_bear_form
    - swipe,name="Swipe"

  # Main — entry point
  main:
    - call_action_list,name=spell_queue
    - call_action_list,name=sanity_checks
    - call_action_list,name=out_of_combat
    - call_action_list,name=auto_target
    - call_action_list,name=auto_heal
    - return,if=auto_combat.player

    # Ensure Bear Form; skip rotation if shifting failed
    - bear_form,name="Bear Form",if=!var.in_bear_form&!buff.feline_potential.up
    - return,if=!var.in_bear_form

    # Combat priority: Interrupts > Defensives > Taunt > Utility > Racials > (valid target check) > CDs > Rotation
    - call_action_list,name=interrupts
    - call_action_list,name=defensives
    - call_action_list,name=taunt
    - call_action_list,name=utility
    - call_action_list,name=racials

    - return,if=!target.exists|target.dead|!target.enemy

    - call_action_list,name=cooldowns
    - call_action_list,name=rotation_elunes
